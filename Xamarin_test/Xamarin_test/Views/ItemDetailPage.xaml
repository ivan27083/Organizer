<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_test.Views.ItemDetailPage"
             xmlns:vm="clr-namespace:Xamarin_test.ViewModels"
             Title="{Binding Title}">
    <StackLayout Spacing="20" Padding="15">
        <Label Text="Text:" FontSize="Medium" TextColor="Blue"/>
        <Label Text="{Binding Text}" FontSize="Small" TextColor="Aqua"/>
        <Label Text="Description:" FontSize="Medium" TextColor="Blue"/>
        <Label Text="{Binding Description}" FontSize="Small" TextColor="Aqua"/>
        <Button Text="Удалить" 
            Command="{Binding deleteItem}"/>
    </StackLayout>
    
</ContentPage>